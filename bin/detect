#!/usr/bin/env bash
# bin/use <build-dir>



echo "Vert.x" && exit 0

if [ -f $1/server.js ]; then
if [ -f $1/server.js ]; then
   echo "Vert.x" && exit 0
elif [ -f $1/Server.java ]; then
   echo "Vert.x" && exit 0
elif [ -f $1/Server.groovy ]; then
   echo "Vert.x" && exit 0
elif [ -f $1/server.rb ]; then
   echo "Vert.x" && exit 0
else
  echo "no" && exit 1
fi
